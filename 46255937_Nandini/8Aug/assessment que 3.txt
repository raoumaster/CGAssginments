que:3
#include<stdio.h>
#include<string.h>
int main()
{
        char l[120];
        printf("enter string:");
        gets(l);
        for(int i=0; l[i]!='\0'; i++)
        {
                if( l[i] >= 'A' && l[i]<= 'Z')
                {
                        l[i] = l[i]+32;
                }
        }
        printf("\n String in lower = %s",l);
        return 0;
}
