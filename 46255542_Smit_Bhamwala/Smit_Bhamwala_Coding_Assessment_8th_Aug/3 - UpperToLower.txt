#include<stdio.h>
int main (){
        char upperChar;
        int lowerChar;

        printf("Enter the character in upper case: ");
        scanf("%c", &upperChar);
        lowerChar = upperChar + 32;
        printf("Lower case is: %c\n", lowerChar);
        return 0;
}