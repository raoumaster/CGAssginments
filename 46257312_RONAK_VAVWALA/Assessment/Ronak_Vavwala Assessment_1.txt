2) MATRIX    
CODE:
#include <stdlib.h>#include <stdio.h>
int main()
{
int n,m;
printf("\n enter the number of rows : ");
scanf(“%d”,&m);
printf("\n enter the number of columns : ");
scanf("%d",&n);
int arr[m][n];
int i,j;
printf("\n Input the matrix \n");
for(i = 0; i < m; i++)
{
for(j = 0; j < n; j++)
{
scanf("%d",&arr[i][j]);
}
}
printf("\nThe Matrix is \n");
for(i = 0; i < m; i++)
{
for(j = 0; j < n; j++)
{
printf("%d ",arr[i][j]);
}
printf("\n");
}
printf("\nSpiral Matrix : ");
int k = 0, l = 0;
while(k < m && l < n)
{
for(i = l; i < n; i++)
{
printf("%d ",arr[k][i]);
}
k++;
for(i = k; i < m; i++)
{
printf("%d ",arr[i][n-1]);
}
n--;
if(k < m)
{
for(i = n-1; i >= 0; --i)
{
printf("%d ",arr[m-1][i]);
}
m--;
}
if(l < n)
{
for(i = m - 1; i >= k; i--)
{
printf("%d ",arr[i][l]);
}
l++;
}
}
return 0;
}

OUTPUT:


3) Prompt the user to enter an upper case letter and display the letter in the lower case.(Don't use any library function)

CODE:

#include <stdio.h>
#include <string.h>
 
int main()
{
char Str1[70];
int i;
  	printf("\n enter a string with upper case letter:  ");
  	gets(Str1);
  	for (i=0; Str1[i]!='\0'; i++)
  	{
  		if(Str1[i] >= 'A' && Str1[i] <= 'Z')
  		{
  			Str1[i] = Str1[i] + 32;
		}
  	}
  	printf("\n the updated lower case string will be: = %s", Str1);
  	
  	return 0;
}


OUTPUT:


