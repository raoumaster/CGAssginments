#include<stdio.h>
#include<stdlib.h>

int main(){
        char ch = 'C';

        printf("letter in upper case:%c\n ",ch);
        ch = ch + 32;
        printf("letter in lower case: %c\n ",ch);

        return 0;
}